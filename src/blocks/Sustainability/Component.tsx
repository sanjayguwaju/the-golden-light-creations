import React from 'react'
import { SustainabilityClient } from './SustainabilityClient'

export const SustainabilityBlock: React.FC<any> = (props) => {
  return <SustainabilityClient data={props} />
}
